# 1. Dosyaları hazırla
# main.py
# requirements.txt
# Procfile

# 2. Git init
git init
git add .
git commit -m "ICTSmartPro API v2.1.0"

# 3. GitHub repo oluştur
git remote add origin https://github.com/USERNAME/ictsmartpro-api.git
git branch -M main
git push -u origin main

# 4. Railway'e git
# - railway.app → Login
# - New Project → Deploy from GitHub
# - Repo seç → Otomatik deploy başlar
